defaults
tls off
account default
host ${HDX_SMTP_ADDR}
port ${HDX_SMTP_PORT}
domain ${HDX_PREFIX}.${HDX_DOMAIN}
from docker.${HDX_TYPE}@${HDX_DOMAIN}
auth off
aliases /etc/aliases
logfile /var/log/msmtp.log
